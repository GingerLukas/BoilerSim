<UserControl x:Class="BoilerSim.BoilerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BoilerSim"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid>
            <Rectangle Fill="Blue" Name="WaterLevel"></Rectangle>
        </Grid>
        <StackPanel Grid.Row="1">
            <TextBlock Name="TemperatureBlock"></TextBlock>
            <TextBlock Name="VolumeBlock"></TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
